{
  "name": "lifecycle",
  "author": "Ariel Gerstein <ariel.gers@gmail.com>",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:server": "lerna run build --scope=common && lerna run start --scope=server"
  },
  "devDependencies": {
    "lerna": "^6.6.1"
  },
  "version": "0.0.1",
  "engines": {
    "node": "18.x"
  }
}
