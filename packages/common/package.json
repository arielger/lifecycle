{
  "name": "@lifecycle/common",
  "version": "1.0.0",
  "scripts": {
    "build": "rimraf ./build tsconfig.tsbuildinfo && tsc --build"
  },
  "dependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^5.0.3"
  }
}
